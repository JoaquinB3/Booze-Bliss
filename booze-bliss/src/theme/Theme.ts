import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#e4ff49",
    },
    secondary: {
      main: "#f43f5e",
    },
    background: {
      default: "#ffff",
    },
    text: {
      primary: "#0A2342",
    },
  },
});

export default theme;
