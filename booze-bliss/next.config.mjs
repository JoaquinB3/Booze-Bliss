/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.thecocktaildb.com', 'th.bing.com', 'media.istockphoto.com', 'images.unsplash.com', 'static.wixstatic.com', 'cloudfront-us-east-1.images.arcpublishing.com'], // Agrega el dominio aquí
    },
};

export default nextConfig;
